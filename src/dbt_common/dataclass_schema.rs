

trait dbtClassMixin{
    """Convert and validate JSON schemas.

    This trait adds methods to generate a JSON schema and
    convert them to and from JSON encodable dicts with validation 
    against the schema
    """

    const _mapped_fields : 
}
